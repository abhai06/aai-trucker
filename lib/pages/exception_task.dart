import 'package:flutter/material.dart';

class ExceptionTaskPage extends StatelessWidget {
  const ExceptionTaskPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Exception'),
      ),
    );
  }
}
